


# 一、整体关系先理清

你现在的建图流程里涉及两类“地图”：

1. **导航栅格地图**
   生成结果通常是：

```bash
0507.pgm
0507.yaml
```

这组文件给 `nav2_map_server` / `map_server` 使用，主要用于 Nav2 导航、RViz 显示地图。

2. **SC 重定位数据库**
   生成结果通常是：

```bash
0507.pcd_db
```

它给 `sc_relocalization` 使用，用于根据点云特征做重定位，最后影响：

```bash
map -> odom
```

所以完整流程不是只保存一个文件，而是：

```text
Adaptive-LIO 建图
        ↓
保存/生成点云地图 pcd
        ↓
pcd2pgm 把 pcd 转成 pgm/yaml 导航地图
        ↓
配置 startup.launch.py 里的 yaml_filename
        ↓
配置 sc_relocalization 的 map_path 指向 .pcd_db
        ↓
启动 Nav2 + RViz + SC 重定位
        ↓
验证 initialpose / tf / 最终位姿是否正确
```

---

# 二、第一步：确认 Adaptive-LIO 开启建图

如果你是用 Adaptive-LIO 重新产图，需要先确认配置文件：

```bash
src/localization/Adaptive-LIO/config/mapping_m.yaml
```

里面需要关注：

```yaml
mapping_module.enable_mapping: true
mapping_module.map_save_path: ...
```

含义是：

```text
enable_mapping: true
```

表示启用建图。

```text
map_save_path
```

决定保存后生成的地图路径和文件名前缀。

图片里提到：代码里最终生成的目录名类似：

```text
map_save_path + "_db"
```

也就是说，如果你的保存路径前缀是：

```bash
/home/sb/origin_nav2026_developing/0507
```

那么可能会生成类似：

```bash
/home/sb/origin_nav2026_developing/0507.pcd
/home/sb/origin_nav2026_developing/0507.pcd_db
```

具体文件名以你程序实际输出为准。

---

# 三、第二步：启动 Adaptive-LIO，让车跑起来建图

你问过：

> 我要启动这个 service 地图还是开始保存吗，还是我车跑完开这个，就会有保存了？

图片里的结论是：

这个 service **不是“启动建图”的开关**，而是**触发一次保存**。

也就是说，正确流程是：

```text
1. enable_mapping: true
2. 启动 Adaptive-LIO
3. 车正常跑，系统一边跑一边建图，地图在内存里持续累积
4. 跑完后，或者中途需要保存时，调用一次保存 service
5. 当前内存中的地图才会落盘保存
```

保存地图的 service 命令是：

```bash
ros2 service call /save_map_service std_srvs/srv/Trigger
```

图片里也提到，配置中可能有：

```yaml
auto_save_interval: 20.0
```

这表示可能会按照距离或间隔自动保存。但为了保险，建议跑完之后再手动调用一次：

```bash
ros2 service call /save_map_service std_srvs/srv/Trigger
```

这样最稳。

---

# 四、第三步：准备 0507.pcd

假设你现在已经有了：

```bash
0507.pcd
```

例如路径是：

```bash
/home/sb/origin_nav2026_developing/0507.pcd
```

后面需要把它转成导航用的：

```bash
0507.pgm
0507.yaml
```

注意，图片里明确说了：

```text
pcd2pgm_node 负责把 PCD 发布成 map 话题；
真正落盘生成 .pgm/.yaml 是 nav2_map_server 的 map_saver_cli 做的。
```

也就是说，`pcd2pgm` 本身更像是把点云地图转成 occupancy grid 并发布出来，而保存成文件需要调用 `map_saver_cli`。

---

# 五、第四步：启动 pcd2pgm

先进入工作空间并加载环境：

```bash
cd /home/sb/origin_nav2026_developing
source install/setup.bash
```

启动 pcd2pgm：

```bash
ros2 launch pcd2pgm pcd2pgm_launch.py
```

图片里提到，默认配置文件是：

```bash
/home/sb/origin_nav2026_developing/src/localization/pcd2pgm/config/pcd2pgm.yaml
```

其中原本的 pcd 文件可能是：

```bash
/home/sb/origin_nav2026_developing/04_17.pcd
```

你现在要改成：

```bash
/home/sb/origin_nav2026_developing/0507.pcd
```

也就是检查或修改 `pcd2pgm.yaml` 里的 pcd 路径，让它指向你当前的 `0507.pcd`。

---

# 六、第五步：保存成 pgm/yaml

另开一个终端，同样 source 环境：

```bash
cd /home/sb/origin_nav2026_developing
source install/setup.bash
```

然后执行保存命令：

```bash
ros2 run nav2_map_server map_saver_cli -f /home/sb/origin_nav2026_developing/0507
```

执行后会生成：

```bash
/home/sb/origin_nav2026_developing/0507.pgm
/home/sb/origin_nav2026_developing/0507.yaml
```

这里注意：

```bash
-f /home/sb/origin_nav2026_developing/0507
```

不要写成：

```bash
-f /home/sb/origin_nav2026_developing/0507.yaml
```

因为 `map_saver_cli` 会自动补全生成：

```bash
0507.pgm
0507.yaml
```

---

# 七、第六步：配置 Nav2 使用新的 yaml 地图

生成完：

```bash
0507.pgm
0507.yaml
```

之后，需要修改启动文件里的导航地图路径。

文件：

```bash
src/bringup/launch/startup.launch.py
```

图片里说这里传给 `map_server` 的参数是：

```python
yaml_filename: .../src/bringup/map/0507.yaml
```

当前值类似：

```python
yaml_filename: .../src/bringup/map/0507.yaml
```

如果你换了新地图，比如：

```bash
0510.yaml
```

就要改成新的 yaml 路径。

并且要注意：

```text
yaml 里还会引用对应的 .pgm，所以 yaml + pgm 要成对存在。
```

例如 `0507.yaml` 里可能有类似：

```yaml
image: 0507.pgm
resolution: ...
origin: ...
negate: ...
occupied_thresh: ...
free_thresh: ...
```

所以必须保证：

```bash
0507.yaml
0507.pgm
```

放入bringup/map 目录下。

---

# 八、第七步：配置 SC 重定位数据库路径


`sc_relocalization` 真正加载数据库的在 launch 参数里。

文件：

```bash
src/localization/sc_relocalization/launch/run.launch.py
```

关键参数：

```python
map_path: ...
```

图片里提到当前默认可能是：

```bash
/home/sb/origin_nav2026_developing/04_17.pcd_db
```

你需要改成新的数据库路径，例如：

```bash
/home/sb/origin_nav2026_developing/0507.pcd_db
```

所以这里应该检查：

```bash
src/localization/sc_relocalization/launch/run.launch.py
```

找到：

```python
map_path
```

然后改成类似：

```python
map_path = '/home/sb/origin_nav2026_developing/0507.pcd_db'
```

或者按照原 launch 文件里的写法修改。

---


# 九、第九步：改完后是否需要重新编译

图片里的结论是：

如果只改：

```text
launch.py
yaml
```

通常**不用重新编译**。

但是需要：

```text
重启 launch，让参数重新加载。
```

如果你改了 C++ / Python 代码本体，才需要：

```bash
colcon build
```

另外图片里还补充了一个例外：

如果你不是用：

```bash
--symlink-install
```

而程序实际读取的是：

```bash
install
```

里面的配置副本，那么你修改 `src` 里的 yaml / launch 之后，可能不会立刻生效。

这时需要同步配置，或者重新编译安装：

```bash
colcon build
source install/setup.bash
```

建议平时开发时使用：

```bash
colcon build --symlink-install
source install/setup.bash
```

这样改 `src` 里的配置更容易直接生效。

---

# 十一、完整命令流程示例：从 0507.pcd 生成 0507.pgm / 0507.yaml

假设你已经有：

```bash
/home/sb/origin_nav2026_developing/0507.pcd
```

完整命令如下。

## 1. 进入工作空间

```bash
cd /home/sb/origin_nav2026_developing
source /opt/ros/jazzy/setup.bash
source install/setup.bash
```

## 2. 修改 pcd2pgm 配置

检查文件：

```bash
src/localization/pcd2pgm/config/pcd2pgm.yaml
```

把里面的 pcd 路径改成：

```bash
/home/sb/origin_nav2026_developing/0507.pcd
```

## 3. 启动 pcd2pgm

```bash
ros2 launch pcd2pgm pcd2pgm_launch.py
```

## 4. 另开终端，保存地图

```bash
cd /home/sb/origin_nav2026_developing
source /opt/ros/jazzy/setup.bash
source install/setup.bash

ros2 run nav2_map_server map_saver_cli -f /home/sb/origin_nav2026_developing/0507
```

生成结果：

```bash
/home/sb/origin_nav2026_developing/0507.pgm
/home/sb/origin_nav2026_developing/0507.yaml
```

---

# 十二、完整配置修改清单

你现在主要要改这些地方。

## 1. Nav2 栅格地图路径

文件：

```bash
src/bringup/launch/startup.launch.py
```

修改：

```python
yaml_filename: .../0507.yaml
```

示例：

```python
yaml_filename: '/home/sb/origin_nav2026_developing/src/bringup/map/0507.yaml'
```

或者按你项目原来的变量结构修改。

---

## 2. SC 重定位数据库路径

文件：

```bash
src/localization/sc_relocalization/launch/run.launch.py
```

修改：

```python
map_path
```

示例：

```python
map_path = '/home/sb/origin_nav2026_developing/0507.pcd_db'
```

---

## 3. SC 算法参数一般不用改

文件：

```bash
src/localization/sc_relocalization/config/relocalization.yaml
```

通常不用动。它主要是：

```text
top_k
scantext
匹配参数
```

不是地图数据库路径。

---

## 4. Adaptive-LIO 建图配置

如果你要重新建图，还要检查：

```bash
src/localization/Adaptive-LIO/config/mapping_m.yaml
```

关注：

```yaml
mapping_module.enable_mapping: true
mapping_module.map_save_path: ...
```

---


# 十四、最终推荐流程总结

你可以按下面这个顺序执行：

```text
1. Adaptive-LIO 中 enable_mapping: true

2. 启动 Adaptive-LIO

3. 车跑完整环境，系统在内存中持续建图

4. 跑完后调用保存：
   ros2 service call /save_map_service std_srvs/srv/Trigger

5. 得到 0507.pcd / 0507.pcd_db

6. 修改 pcd2pgm.yaml，让它读取：
   /home/sb/origin_nav2026_developing/0507.pcd

7. 启动：
   ros2 launch pcd2pgm pcd2pgm_launch.py

8. 另开终端保存 pgm/yaml：
   ros2 run nav2_map_server map_saver_cli -f /home/sb/origin_nav2026_developing/0507

9. 得到：
   0507.pgm
   0507.yaml

10. 修改 startup.launch.py：
    yaml_filename 指向 0507.yaml

11. 修改 sc_relocalization/launch/run.launch.py：
    map_path 指向 0507.pcd_db

12. 重启 launch

13. 用下面命令验证：
    ros2 topic echo /initialpose --once
    ros2 run tf2_ros tf2_echo map odom

14. 对比 red / blue：
    - initialpose 不同但 map->odom 收敛趋同：正常，说明重定位覆盖
    - initialpose 本身一样：启动分支/参数没切对
```

---

# 十五、关键命令汇总

```bash
cd /home/sb/origin_nav2026_developing
source /opt/ros/jazzy/setup.bash
source install/setup.bash
```

保存 Adaptive-LIO 地图：

```bash
ros2 service call /save_map_service std_srvs/srv/Trigger
```

启动 pcd2pgm：

```bash
ros2 launch pcd2pgm pcd2pgm_launch.py
```

保存 pgm/yaml：

```bash
ros2 run nav2_map_server map_saver_cli -f /home/sb/origin_nav2026_developing/0507
```

查看初始位姿：

```bash
ros2 topic echo /initialpose --once
```

查看 TF：

```bash
ros2 run tf2_ros tf2_echo map odom
```

验证是否有保存服务：

```bash
ros2 service list | grep save
```

查看地图话题是否存在：

```bash
ros2 topic list | grep map
```

查看 `/map` 是否有数据：

```bash
ros2 topic echo /map --once
```

---

# 十九、你现在最应该检查的三个文件

```bash
src/localization/pcd2pgm/config/pcd2pgm.yaml
```

确认它读的是：

```bash
/home/sb/origin_nav2026_developing/0507.pcd
```

---

```bash
src/bringup/launch/startup.launch.py
```

确认：

```python
yaml_filename
```

指向新的：

```bash
0507.yaml
```

---

```bash
src/localization/sc_relocalization/launch/run.launch.py
```

确认：

```python
map_path
```

指向新的：

```bash
0507.pcd_db
```

---

整体来说，你的流程核心可以记成一句话：

```text
车跑起来时 Adaptive-LIO 在内存里建图；跑完后调用 /save_map_service 保存 pcd/pcd_db；再用 pcd2pgm + map_saver_cli 生成 pgm/yaml；最后分别把 Nav2 的 yaml_filename 和 SC 的 map_path 指到新地图。
```
